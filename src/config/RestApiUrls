const authPort = "9595";
const userPort = "8091";
const postPort = "8092";
const version = "/api/v.0.1";

const RestApiUrls = {
    authService: 'http://localhost:' + authPort + version,
    userService: 'http://localhost:' + userPort + version,
    postService: 'http://localhost:' + postPort + version,
};

export default RestApiUrls;